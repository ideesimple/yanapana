<% current_order.line_items.each do |item| %>
  <% image = item.variant.product.images.first %>
  <li><%= image_tag(image.attachment.url(:mini)) %><a href="#"><%= item.variant.product.name %></a>
    <span class="close fr"><a href="#"></a></span>
    <span class="price"><%= number_to_currency(item.price)  %></span>
  </li>
<% end %>

<div class="cart_bottom">
  <div class="subtotal_menu">
    <small>Subtotal:</small><big><%= current_order.display_total%></big>
  </div>
  <% unless action_name == 'preview_cause' || action_name == 'preview_product' || action_name == 'populate' %>
    <%= link_to "Checkout", cart_path %>
  <% end %>
</div>

