<% content_for :head do %>
<script>
  $(document).ready(function() {

	    $('.add_cart').click(function(){
            $('html, body').animate({scrollTop:0}, 'slow');
            return true;

            location.reload();
	    });

	});
</script>

<!--Este script sirve para colapsar el quick overview-->
  <script type="text/javascript">
    $(function()
    {

      $("#mostrar").click(function(event) {
        event.preventDefault();
        $("#caja").slideToggle();
      });

      $("#caja a").click(function(event) {
        event.preventDefault();
        $("#caja").slideUp();
      });
    });
  </script>


<% end %>


	   <!-- <div class="zoom-container layout_default">
	          <div class="main-image">
			    <%= link_to (product_image(@product, :itemprop => "image") ), @product.images.first.attachment.url(:large), :class => "main-thumbnail", :id => "zoom"  %>
			    <div class="lightbox-btn">
			      <%= link_to "Agrandar",@product.images.first.attachment.url(:large), :class => "lightbox", :id => "zoom" %>
			    </div>
			  </div>
			  <div class="more-views ">
			    <div class="zoom-gallery slider">
			    <% @product.images.each do |i| %>
			      <div class="slide">
			        <%= link_to (image_tag i.attachment.url(:mini)), i.attachment.url(:large), {"class" => "zoom-thumbnail thumbnail-active", "data-easyzoom-source" => i.attachment.url(:product) } %>
			      </div>
			    <% end %>
			    </div>
			  </div>
            </div>-->

<div class="section_container jean">
  <section id="product-section">
    <ul class="breadcrumb">
      <li><%= link_to "Home", root_path %></li>
      <li class="active">Product Detail</li>
    </ul>

    <div id="shopping_cart" class="full_page">
      <div class ="message"><%= flash[:error]  %></div>
      <div class="message success" style="display:none">
        Success :D <a href="#" class="fr"></a>
      </div>

    </div>

    <!--PRODUCT DETAIL STARTS-->
    <div id="product_detail">
      <!--Product Left Starts-->
      <div class="product_leftcol">
        <%= link_to (product_image(@product, :itemprop => "image") ), @product.images.first.attachment.url(:large), :class => "rollover-zoom", :title => "yanapana"  %>

        <!-- <span class="pr_info">ROLL OVER IMAGE TO ZOOM</span> -->
        <ul class="pr_gallery">
        <% @product.images.each do |i| %>
          <%= link_to (image_tag i.attachment.url(:small)), i.attachment.url(:large), {"class" => "zoom-thumbnail thumbnail-active", "data-easyzoom-source" => i.attachment.url(:product) } %>
        <% end %>
        </ul>
      </div>

      <!--Product Left Ends-->

      <!--Product Right Starts-->
      <div class="product_rightcol">
        <h1><%= @product.name %></h1>
        <% if @product.description %>
          <p class="short_dc"><%= @product.description %></p>
        <% end %>

         <%#= render :partial => "cart_form" %>

         <%= form_for :order, :url => populate_orders_path do |f| %>

           <% if @product.price %>
             <div class="pr_price">
               <% if @product.price %>
                 <big><%= number_to_currency @product.price %></big><small></small>
               <% end %>
             </div>

             <%= render :partial => "variants" %>

             <div class="qty_info">
               <% if @product.on_sale? %>
                 <div class="quantity">
                   <label>Quantity</label>
                   <%= number_field_tag (@product.has_variants? ? :quantity : "variants[#{@product.master.id}]"),
                     1, :class => 'title', :in => 1..@product.on_hand, :min => 1 %>



                   <div class="add_to_buttons">
                     <a href="#top"></a>
                     <%= button_tag :class => 'add_cart', :id => 'add-to-cart-button', :type => :submit do %>
                       <%= t(:add_to_cart) %>
                     <% end %>
                   </div>
                 <% else %>
                   <%= content_tag('strong', t(:out_of_stock)) %>
                 </div>
               <% end %>
             </div>

           </div>
         <% end %>


       <% end %>

         <div class="product_overview">
            <!--ORG Logo Starts-->
            <div>
              <!--ORG Logo Ends-->
              <!--ORG Description Starts-->
              <h4>Organization</h4>
              <div class="org_logo"><%= link_to (image_tag(@product.cause.organization.photo.url(:org))), root_path %></div>
              <!--Facebook like button Starts-->
              <div class="fb-like" data-send="false" data-layout="button_count" data-width="450" data-show-faces="true"></div>

              <!--Facebook like button Ends-->

              <!--Twitter follotw button Starts-->

              <div class="follow-btn"><a href="https://twitter.com/StandUpCancer" class="twitter-follow-button" data-show-count="false" data-show-screen-name="false" data-dnt="true">Follow @StandUpCancer</a></div>

              <!--Twitter follotw button Ends-->

            </div>

            <!--ORG Description Ends-->

          </div>

          <div class="product_overview hidden_movil">
            <%= render :partial => "properties" %>

          </div>
        </div>
        <!--Product Right Ends-->
      </div>
      <!--PRODUCT DETAIL ENDS-->
    </section>

  <div id="artist-section" class="artist-inspiration jean">
    <section>
      <h2 class="sub_title">The Artist</h2>
      <div class="artist"><a href="#"><%= image_tag ("image-yanapana/artist.jpg") %></a></div>
      <div class="artist_description">
        <h2 class="sub_title">Inspiration Behind</h1>
        <p><%= @product.inspiration_behind  %></p>
      </div>

      <!--Facebook like button Starts-->
      <div class="fb-like" data-send="false" data-layout="button_count" data-width="450" data-show-faces="true"></div>
      <!--Facebook like button Ends-->
      <!--Twitter follotw button Starts-->
      <div class="follow-btn"><a href="https://twitter.com/StandUpCancer" class="twitter-follow-button" data-show-count="false" data-show-screen-name="false" data-dnt="true">Follow @StandUpCancer</a></div>
      <!--Twitter follotw button Ends-->
    </section>
  </div>
</div>


