
<thead>
  <tr data-hook="cart_adjustments_headers">
    <th class="cart-adjustment-header" colspan="6"><%= t(:order_adjustments) %></th>
  </tr>
</thead>
<tbody id="cart_adjustments" data-hook>
  <% @order.adjustments.eligible.each do |adjustment| %>
    <tr>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td class="align_left" colspan="4"><%= adjustment.label %></td>
      <td class="align_right"><%= money(adjustment.amount) %></td>
      <td></td>
    </tr>
  <% end %>
</tbody>

